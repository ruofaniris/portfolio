$(document).ready(function(){
           $('.aniview').AniView();
       });
$('.aniview').AniView();
var options={
  animateThreshold: 100,
  scrollPollInterval:15,
}
$('.aniview').AniView(options);
